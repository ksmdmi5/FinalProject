import { ToolAvailablePipe } from './tool-available.pipe';

describe('ToolAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new ToolAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
